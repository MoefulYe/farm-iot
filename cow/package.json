{
  "scripts": {
    "protoc": "for file in ../proto/cow/*;do name=`basename ${file}`; pbjs --ts protoc/${name%%.*}.ts ${file};done"
  },
  "dependencies": {
    "@types/uuid": "^9.0.7",
    "mqtt": "^5.3.3",
    "uuid": "^9.0.1",
    "winston": "^3.11.0"
  }
}
